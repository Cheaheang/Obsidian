**Backend**
- Use snake-case
- don't use data, when return
- success status return with 201
**Frontend**
- use camelCase, when create file
- In **Vue** declare variable in must following this format:  name + Store
```js
productStore = useProduct()
```

**Postman**
- When create folder, put name of it endpoint
- inside the folder, should have crud and put name as it is(create, read, update, delete, )




**Variable Sample**
```js
const boolean = ref(false)
const string = ref("hello world")
const number  = ref(2)
const array = ref(['3','4',4])
//to access array: array[0]
const object = refI+({count: 0})
//to access object: object.count
```

